/** DOM nodeType values — use instead of global `Node` (undefined in Node.js without DOM polyfill). */
export const DOM_NODE_ELEMENT = 1;
export const DOM_NODE_TEXT = 3;
export const DOM_NODE_COMMENT = 8;
