/**
 * Landing typography: SF Pro files in `src/app/fonts/sf-pro-display/` are loaded via
 * `@font-face` in `globals.css`; `.marketing-surface` sets `--font-marketing-sans`.
 */
export const marketingLandingFontClassName = "font-sans";
